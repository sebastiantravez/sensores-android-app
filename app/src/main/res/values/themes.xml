<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Sensoreandroidapp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/brown_500</item>
    </style>

    <style name="SplashTheme" parent="Theme.Sensoreandroidapp">
        <item name="android:windowBackground">@drawable/splash_up</item>
        <item name="colorPrimary">@color/white</item>
    </style>

</resources>